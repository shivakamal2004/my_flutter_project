import 'package:flutter/material.dart';

class FlightScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Flight Details'),
      ),
      body: Center(
        child: Text('Flight details go here...'),
      ),
    );
  }
}
