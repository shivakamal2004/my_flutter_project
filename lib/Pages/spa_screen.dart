import 'package:flutter/material.dart';

class SpaScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Spa Details'),
      ),
      body: Center(
        child: Text('Spa details go here...'),
      ),
    );
  }
}
