import 'package:flutter/material.dart';

class HotelsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hotels Details'),
      ),
      body: Center(
        child: Text('Hotels details go here...'),
      ),
    );
  }
}
